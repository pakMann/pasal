# Optional heavy dependencies for semantic search (search_laws_semantic).
# Keep out of requirements.txt so the base Railway deploy stays light — the
# tool degrades gracefully to full-text search when these are not installed.
# Must match the embedding model/version used by scripts/embed/.
# Install with: pip install -r requirements-semantic.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2,<3
numpy>=1.26
FlagEmbedding>=1.3.3
